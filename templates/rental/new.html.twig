{% extends 'base.html.twig' %}

{% block title %}New Rental{% endblock %}

{% block body %}
    <h1>Create new Rental</h1>

   {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Réserver') }}</button>
{{ form_end(form) }}

    <a href="{{ path('app_vehicles') }}">back to list</a>
{% endblock %}
